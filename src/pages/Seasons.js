import React from "react";
import Calendar from "../components/Calendar";

const Seasons = () => {
    return (
        <>
            <div className="mx-auto h-screen w-screen bg-gray-200">
                <Calendar />
            </div>
        </>
    );
}

export default Seasons;